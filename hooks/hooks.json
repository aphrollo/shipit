{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "cat \"${CLAUDE_PLUGIN_ROOT}/skills/router/SKILL.md\" 2>/dev/null || echo 'shipit router not found'",
            "async": false
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'SHIPIT ROUTER: If this prompt requests code/config/infra changes (build, fix, change, add, remove, refactor, create, update, implement, debug, deploy), you MUST invoke the Skill tool with skill: \"shipit:router\" BEFORE generating any code or response. Classify the task, then dispatch via /orchestrate. Do NOT skip this step. Do NOT respond inline. If the prompt is a question, greeting, or non-work request, respond normally. TELEGRAM: If a Telegram chat_id exists in this conversation, send short progress updates via telegram__reply at each phase transition (agent start/pass/fail). Format: emoji + phase + one-line status. Do not skip reporting.'",
            "async": false
          }
        ]
      }
    ]
  }
}
